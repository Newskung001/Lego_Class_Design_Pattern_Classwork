# Lego Game - Design Pattern Project

โปรเจกต์นี้เป็นส่วนหนึ่งของรายวิชา GDM 330 GAME DESIGN AND DEVELOPMENT WORKSHOP II โดยมีวัตถุประสงค์เพื่อนำ Pattern ที่เรียนมาประยุกต์ใช้ในการ Refactor เกม Lego Land ให้มีโครงสร้างโค้ดที่ยืดหยุ่น อ่านง่าย และรองรับการขยายตัวในอนาคต

## 📋 รายละเอียดโปรเจกต์
- **Engine:** Unity 202x
- **Language:** C#
- **Developer:** ภาณุพงศ์ นุ่มพูล 66120501028

## 🛠 Design Patterns ที่ใช้

โปรเจกต์นี้ได้นำ Design Patterns มาใช้ทั้งหมด 3 รูปแบบ ดังนี้:

### 1. Observer Pattern
- **ปัญหาเดิม:** `Pickup.cs` (ไอเทม) ต้องอ้างถึง `ScoreController` โดยตรงเพื่อเพิ่มคะแนน ทำให้เกิด High Coupling
- **การแก้ไข:** สร้าง `EventManager` ทำหน้าที่เป็นคนกลาง (Subject) ประกาศ Event ต่างๆ
- **การทำงาน:** - เมื่อผู้เล่นเก็บไอเทม `Pickup` จะประกาศ Event `OnCoinCollected`
  - เมื่อผู้เล่นชนะ `ScoreController` จะประกาศ Event `OnPlayerWin`
  - Class อื่นๆ เช่น UI หรือ Boss สามารถ Subscribe เพื่อรับรู้เหตุการณ์ได้โดยไม่ต้องผูกมัดกันโดยตรง
- **ประโยชน์:** ลดการผูกมัดระหว่าง Class (Decoupling) ตามหลัก OCP

### 2. Command Pattern
- **ปัญหาเดิม:** การทำระบบ Power-Up (เพิ่มความเร็ว) มักเขียน Logic ปนอยู่ใน `PlayerController` ยากต่อการขยายและทำระบบ Undo
- **การแก้ไข:** แยกการกระทำ Power-Up ออกเป็น Class `PowerUpCommand` ที่สืบทอดจาก `ICommand`
- **การทำงาน:** - `CommandManager` ทำหน้าที่เก็บประวัติคำสั่งลงใน Stack
  - **Execute:** สั่งให้ Player เพิ่มความเร็ว
  - **Undo:** ยกเลิกผลของ Power-Up ทันที
- **ประโยชน์:** รองรับระบบ Undo/Redo และแยก Logic การกระทำออกจากตัวผู้เล่น (SRP)

### 3. State Pattern
- **ปัญหาเดิม:** AI ของ Boss ใช้ตัวแปร Boolean (`isWalking`, `isAngry`) จำนวนมากใน `Update` เพื่อเช็คสถานะ ทำให้โค้ดซับซ้อนและแก้ยาก
- **การแก้ไข:** แยกพฤติกรรมแต่ละสถานะเป็น Class แยกกัน ได้แก่:
  - `BossIntroState`: เดินเข้าฉาก
  - `BossChaseState`: ไล่ล่าผู้เล่น
  - `BossEnrageState`: โกรธและวิ่งเร็วขึ้น
  - `BossPlayerWinState`: หยุดการทำงานเมื่อผู้เล่นชนะ
- **การทำงาน:** `BossController` จะมี `StateMachine` คอยสลับสถานะตามเงื่อนไข (เช่น หมดเวลา หรือ ผู้เล่นชนะ) โดยแต่ละไฟล์ State ถูกแยกออกจากกันเพื่อความสะอาดของโค้ด
- **ประโยชน์:** เพิ่มสถานะใหม่ๆ ให้บอสได้ง่ายโดยไม่ต้องแก้โค้ดเดิม และลดการใช้ if-else ซ้อนกัน

## 🎮 วิธีการเล่นและทดสอบ
1. **การเคลื่อนที่:** คลิกเมาส์ซ้ายบนพื้นเพื่อเดิน
2. **เก็บเหรียญ:** เดินชนแท่ง Lego สีทองเพื่อเก็บคะแนน (ทดสอบ Observer)
3. **Power-Up:** เดินชนแคปซูล Power-Up เพื่อเพิ่มความเร็ว
4. **Undo/Redo:** กดปุ่ม **U** เพื่อ Undo ความเร็วกลับเป็นปกติ และกด **R** เพื่อ Redo (ทดสอบ Command)
5. **Boss:** สังเกตพฤติกรรมบอสที่จะเริ่มจากเดินเปิดตัว -> วิ่งไล่ -> และโกรธ (ทดสอบ State)

---
